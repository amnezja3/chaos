# Capture SFX assets

Expected production MP3 files:

- `01_target.mp3` — ordinary captured map target;
- `02_conflict_pillar.mp3` — captured canonical conflict pillar;
- `03_conflict_resolved.mp3` — canonical conflict resolution.

Missing files fail silently through the shared GameSfx negative cache and never
affect capture, territory rebuilds or conflict processing.
